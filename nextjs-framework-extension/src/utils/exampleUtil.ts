export const exampleUtilFunction = (input: string): string => {
    return `Processed: ${input}`;
};

export const anotherUtilityFunction = (array: number[]): number[] => {
    return array.map(item => item * 2);
};